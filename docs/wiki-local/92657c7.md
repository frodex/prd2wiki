# PLAN: prd2wiki + pippi-librarian Code Consolidation — Pre-flight Cleanup

**Date:** 2026-04-12
**Status:** Ready to execute
**Depends on:** Nothing — this is pre-flight
**Blocks:** [Master Plan](http://192.168.22.56:8082/projects/default/pages/8634f02) Phase 1+
**Cross-repo constraints:** `/srv/prd2wiki/docs/constraints-prd2wiki-pippi.md`

## Workspace — Everything Is Local

**There is no GitHub. No remote repos. No cloning. Both codebases are already on this machine:**

| Repo | Path | Git status |
|------|------|-----------|
| **prd2wiki** | `/srv/prd2wiki/` | Git repo on `main`. Tests pass. Branch from here. |
| **pippi-librarian** | `/srv/pippi-librarian/` | Code on disk, **no `.git`** (extracted from tarball). Run `git init` before branching. |

**Wiki pages** are at `http://192.168.22.56:8082/` (LAN). Read via MCP `wiki_read` tool or browser.

**Do not `git clone` or `git pull` anything.** The code is already here.

## Related Wiki Pages

- [Master Plan](http://192.168.22.56:8082/projects/default/pages/8634f02) — execution overview, build strategy
- [File Map](http://192.168.22.56:8082/projects/default/pages/97a0970) — all file paths, build commands
- [Unified Identity](http://192.168.22.56:8082/projects/default/pages/de836ff) — tree design, .uuid/.link format
- [Phase 3 Expansion](http://192.168.22.56:8082/projects/default/pages/56803d5) — tree router, UI, security, testing

## Where to Work

| Phase | Path | Branch | Start when |
|-------|------|--------|-----------|
| **Items 1-5** | `/srv/prd2wiki/` | `impl/preflight-a` | **Now** |
| **Items 6-7** | `/srv/pippi-librarian/` | `impl/preflight-a` (after `git init`) | **Now** (parallel) |
| **Items 8-11** | `/srv/prd2wiki/data/` + new `tree/` | Scripts, not branch | **Maintenance window — wiki stopped** |

**Branch first:** `git checkout -b impl/preflight-a` in `/srv/prd2wiki/`. Do not work on `main`.

**Tests:** `go test ./...` must pass after each item. Current state: all pass on main.

---

## Phase A: Code Cleanup (prd2wiki — `/srv/prd2wiki/`)

### Item 1: Librarian submit — 3 methods → 1

**File:** `internal/librarian/librarian.go` lines 276-409

**Before:** `submitVerbatim`, `submitConform`, `submitIntegrate` — 130 lines, 90% duplicated.

**After:** One `submit()` method. Intent drives behavior via flags. `SubmitResult` gains `CommitHash` field. `SubmitRequest` gains `PageUUID` and `ProjectUUID` fields (optional — empty string until tree migration, no-op when empty).

**Gate:** `go test ./internal/librarian/` passes. All 3 intents work via wiki_propose MCP tool.

---

### Item 2: API project validation — helper

**Files:** `internal/api/pages.go`, `history.go`, `attachments.go`, `references.go`, `search.go`

**After:** `projectRepo()` helper returns repo or writes error.

**Gate:** `go test ./internal/api/` passes.

---

### Item 3: JSON response helper

**Files:** All in `internal/api/`

**After:** New file `internal/api/respond.go` with `writeJSON(w, status, data)`.

**Gate:** `go test ./internal/api/` passes.

---

### Item 4: resolvePagePath — shared function

**Files:** `internal/api/server.go`, `internal/web/handler.go`

**After:** New package `internal/pagepath/resolve.go`. Both API and web import from it.

**Gate:** `go test ./...` passes.

---

### Item 5: Git write consolidation + commit hash return + FirstCommitDate

**Files:** `internal/git/repo.go`, `page.go`

**After:** One `writePage()` returns `(string, error)` with commit hash. `FirstCommitDate()` helper.

**Gate:** `go test ./internal/git/` passes.

---

## Phase A: Code Cleanup (pippi-librarian — `/srv/pippi-librarian/`)

**First:** `cd /srv/pippi-librarian && git init && git add -A && git commit -m "initial: pippi-librarian as extracted"`

### Item 6: Merge entity table into memory table

**Files:** `internal/librarian/entity_lance.go` (DELETE), `entity_lance_stub.go` (DELETE), `entities.go`, `librarian.go`, `memory_lance.go`, `cmd/pippi-librarian/main.go`

**After:** One table (`pippi_memory`). One prefix (`mem_`). `record_type` column. `page_uuid = ""` for entities.

**Build/test:**
```bash
cd /srv/pippi-librarian
LD_LIBRARY_PATH=lib/linux_amd64 CGO_ENABLED=1 CGO_LDFLAGS="-Llib/linux_amd64 -llancedb_go" go build ./...
LD_LIBRARY_PATH=lib/linux_amd64 CGO_ENABLED=1 CGO_LDFLAGS="-Llib/linux_amd64 -llancedb_go" go test ./internal/librarian/ -race -count=1
```

**Gate:** Tests pass including `-race`.

---

### Item 7: Remove dead race code

**File:** `internal/librarian/memory.go`

**Delete:** `GetWithRace()`, `GetRaceResult`, `getFromSQLite()`, `getBackendResult`.

**Gate:** Tests pass.

---

## Phase B: Tree Migration (MAINTENANCE WINDOW)

**Stop the wiki before running Phase B.** All items touch `/srv/prd2wiki/data/` and create `/srv/prd2wiki/tree/`.

### Item 8: Assign UUIDs to projects
Create `tree/` directory. Write `.uuid` files.

### Item 10: Rename git repos
`data/{name}.wiki.git/` → `data/repos/proj_{uuid8}.git/`

### Item 9: Create .link files + migrate page IDs
UUID per page, rename git files to `pages/{uuid}.md`, create `.link` files.

### Item 9b: Rebuild index.db
Delete `index.db`. Wiki rebuilds on startup.

### Item 11: Extract blobs
Binary attachments → `data/blobs/{sha256}`.

---

## Success Criteria

### Phase A — prd2wiki (items 1-5)
- [ ] One submit path with `CommitHash` + optional `PageUUID`/`ProjectUUID`
- [ ] `projectRepo()`, `writeJSON()`, `pagepath.Resolve()` helpers
- [ ] `WritePage()` returns `(string, error)`
- [ ] `go test ./...` passes

### Phase A — pippi-librarian (items 6-7)
- [ ] One table, `mem_` IDs, `record_type`, `page_uuid = ""`
- [ ] Race code deleted
- [ ] `go test -race` passes

### Phase B — data migration (items 8-11)
- [ ] `.uuid` + `.link` files exist
- [ ] Repos + pages renamed to UUID convention
- [ ] `index.db` rebuilt, blobs extracted
- [ ] Wiki starts and serves pages
